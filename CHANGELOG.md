# 更新记录

本文件记录项目中用户可感知的功能、排版、部署和维护流程变化。日期使用项目修改当天的本地日期。

## 2026-08-18

### 新增

- 接入百度通用文字识别：两张票据图片齐全后自动生成临时 A4 合成图，通过后端安全代理识别金额，并自动填入销售金额输入框。
- 新增 `POST /api/ocr/amount`，支持 PNG/JPEG 二进制请求、百度 access token 缓存、语义金额提取、请求体限制、频率限制和并发限制。
- 新增 `backend/.env.example` 和本机环境变量加载器；真实百度凭据仅保存在 gitignored 的 `backend/.env` 或后端进程环境变量中。

### 变更

- 下载完整组合图的文件名改为 `票据YYYYMMDDHHmmss.png`，使用点击下载时的本地年月日时分秒。
- OCR 识别过程中替换或清除图片会取消旧请求；用户手动修改过金额时，不使用迟到的识别结果覆盖人工输入。

### 修复

- 修复 OCR 自动回填可能覆盖识别前已有手工金额，或覆盖“识别期间修改后又改回”的人工操作；现在使用显式编辑版本保护手工值。
- 修复订单号等同行数字可能被误判为金额，以及无语义标签时从任意小数中猜测金额的问题；歧义结果现在返回 `422` 并要求手动输入。
- 修复 OCR 并发槽位可能在请求体解析期间被绕过、浏览器取消后百度请求仍继续、前后端 30 秒超时相互竞争，以及失效 token 并发重复刷新的问题；现在采用同步占位、端到端取消、35 秒服务端整体期限、45 秒浏览器等待窗口和共享刷新锁。
- 修复 1920×1080 屏幕在浏览器缩放为 125% 或 150% 时，`1179×3558` 商务存根未稳定贴底、`1179×2328` 购物小票比例或位置偏移的问题。
- 屏幕预览改为由浏览器使用 `object-fit: contain` 按原始宽高比绘制；存根固定底部对齐，小票固定顶部对齐。
- 打印和下载在 html2canvas 克隆中使用屏幕实际可见矩形的明确坐标和尺寸，避免 html2canvas 1.4.1 不支持 `object-fit` 导致输出与预览不一致。

### 运维

- 新增项目级自动发布规则：每次修改完成后执行相关验证、同步 README、更新本文件、创建提交并推送当前远程分支。
- 自动推送保留安全例外：验证失败、存在敏感或非预期文件、远程冲突或需要强制推送时停止并报告。